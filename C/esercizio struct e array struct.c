
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define MAX = 100;

typedef struct {
	char cognome[50];
	char nome[50];
	float voti[15];
} Alunno;


int main(){

	Alunno studente;
	studente.voti[0] = 6;
	strcpy(studente.cognome, "rossi");
	strcpy(studente.nome, "marco");
	alunno classe [MAX];

	for(int k=0, k<MAX, k++){

		classe[k].voti[0] = 6;
		strcpy(classe[i].cognome, "rossi");
		strcpy(classe[i].nome, "marco");

	}

}

